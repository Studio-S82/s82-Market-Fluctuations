Note:

Config.Items = {
	normal = {
		steel = { -- Name item
			Min = 100, -- base price min
			Max = 120, -- base price max
			AmountToChange = 2000 -- The number of items purchased will reset the base price.
		},
	},
};
